{
  "name": "Forward",
  "version": "0.1.0",
  "description": "Brainstorming on how to build request processing pipelines in Node",
  "main": "./dist/Examples/server.js",
  "scripts": {
    "test": "npm run build && mocha --recursive dist/test",
    "build": "tsc -p .",
    "watch": "tsc -p . --watch",
    "typings": "typings install"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "chai": "^3.5.0",
    "commander": "^2.9.0",
    "http2": "^3.3.2",
    "mocha": "^2.4.5",
    "typescript": "^1.8.9",
    "typings": "^0.7.9",
    "winston": "2.2.0"
  },
  "dependencies": {
    "restify": "^4.0.3"
  }
}
