// import * as restify from 'restify';
// import * as continua from './generated';


// const serviceBrokerUrl = process.env.serviceBrokerUrl || "http://localhost:8000";

// var continuum = new continua.Continuum(serviceBrokerUrl);
    
// var server = restify.createServer();
// server.post('/api/test', (req,res,next) => {    
//     var keyToLookup = { key: "your_favorite_key" }
//     continuum.forward(continuum.cacheStage, keyToLookup, (continuum, stage, state) => stage.getDataAndProcess(continuum, stage, state));
// });

// server.listen(8000);

